<html>
  <head>
    <title>Your Booking is Confirmed!</title>
    <style>
      body { font-family: Arial, sans-serif; margin: 0; padding: 0; } .ticket {
      max-width: 500px; margin: 0 auto; border: 1px solid #ddd; padding: 20px;
      border-radius: 5px; } .header { text-align: center; margin-bottom: 20px; }
      .route-info { display: flex; justify-content: space-between; margin: 20px
      0; background-color: #f5f5f5; padding: 15px; border-radius: 5px; }
      .ticket-details { margin: 20px 0; } .passenger-info { margin-top: 20px;
      padding-top: 20px; border-top: 1px solid #ddd; }
    </style>
  </head>
  <body>
    <div class="ticket">
      <div class="header">
        <img src="cid:nrc-logo" alt="NRC Logo" width="150" />
        <h2>Your Booking is Confirmed! 🎉</h2>
        <p>Thank you for choosing Nigerian Railway Corporation. Your ticket
          details are below.</p>
      </div>

      <div class="route-info">
        <div>
          <p><strong>{{departureTime}}</strong></p>
          <p>{{departureStation}}</p>
          <p>{{departureDate}}</p>
        </div>
        <div>
          <p><strong>{{arrivalTime}}</strong></p>
          <p>{{arrivalStation}}</p>
          <p>{{arrivalDate}}</p>
        </div>
      </div>

      <div class="ticket-details">
        <p><strong>Train Number:</strong> {{trainNumber}}</p>
        <p><strong>Coach Number:</strong> {{coachNumber}}</p>
        <p><strong>Seat Number:</strong> {{seatNumber}}</p>
        <p><strong>Passenger Type:</strong> {{passengerType}}</p>
        <p><strong>Booking ID:</strong> {{bookingId}}</p>
        <p><strong>Booking Date:</strong> {{bookingDate}}</p>
      </div>

      <div class="passenger-info">
        <h3>Passenger Details</h3>
        <p><strong>Full Name:</strong> {{passengerName}}</p>
        <p><strong>NIN:</strong> {{passengerNIN}}</p>
        <p><strong>Email:</strong> {{passengerEmail}}</p>
        <p><strong>Phone:</strong> {{passengerPhone}}</p>
      </div>
    </div>
  </body>
</html>